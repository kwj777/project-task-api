[project]
name = "project-task-api"
version = "0.2.0"
description = "Course demonstration API for managing projects and tasks"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.116.0",
]
